# 📋 第 1 周试点执行表（2026/08/15 周六 → 08/21 周五）

> 所属阶段：阶段一·工程底座（8/15–8/31，共 16 个实质天，~136h）
> 本周主题：**Python 工程化（L3）+ 异步/测试 + FastAPI + MySQL 深化（上半）**
> 本表为**试点**：执行完第 7 天晚上按"第七节·周复盘"评估，效果 OK 再生成后续各周。
> 每日主线 8.5h + 背景线 1.2h（算法 1 题 + 单词 30min，碎片时间完成，不占主线）。

---

## 一、每日时间分配模板（8.5h 主线）

| 时段 | 时长 | 内容 |
|------|:---:|------|
| 上午 | 3h | 学新知识（读文档/教程，**边看边敲示例代码**） |
| 下午 | 3h | 动手练习（当天知识的独立练习 + 提交 git） |
| 晚上 | 2.5h | 自测/笔记/补缺 + 周中自测题 + 复盘记录 |

> 原则：**练习时间 ≥ 学习时间**。看懂了不算会，写出来跑通才算。
> 资源使用比例：概念入门 20%（中文教程）→ 深度理解 30%（官方文档）→ **动手练习 50%**。

---

## 二、本周每日任务

### Day 1 ｜ 8/15 周六 ｜ Python 装饰器与函数进阶（L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① 函数一等公民、闭包（为什么需要闭包、变量捕获）② 装饰器原理：`@deco` ≡ `func = deco(func)` ③ 无参/带参/类装饰器、`functools.wraps` | 3h |
| 练习 | 写 4 个装饰器：日志装饰器、执行计时、带参重试（最多 N 次）、`@cache` 简易缓存（functools 版）；全部提交 git | 3h |
| 自测 | 白板解释：装饰器执行顺序；`@wraps` 为什么必要；装饰器 vs 装饰器工厂区别 | 2h |
| 产出 | `notes/day01-装饰器.md` + 练习代码 | — |

> **资源**：入门读廖雪峰《装饰器》→ `https://liaoxuefeng.com/books/python/function/decorator.html`；L3 深度看 Python 官方 Functional Programming HOWTO（迭代器/生成器/闭包原理）→ `https://docs.python.org/3/howto/functional.html`；备选：菜鸟教程装饰器 → `https://www.runoob.com/python3/python3-decorators.html`

### Day 2 ｜ 8/16 周日 ｜ 生成器与迭代器（L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① 迭代器协议（`__iter__`/`__next__`）、可迭代 vs 迭代器 ② `yield`/`yield from`/`send()` ③ 生成器表达式、惰性求值、内存优势 | 3h |
| 练习 | 斐波那契生成器、大文件流式逐行读取、无限序列 `take(5)`；对比列表占内存 | 3h |
| 自测 | 解释：`range` vs `list` 内存；生成器一次消费原理；`send` 的用途 | 2h |
| 产出 | `notes/day02-生成器.md` | — |

> **资源**：入门读廖雪峰《生成器》→ `https://liaoxuefeng.com/books/python/function/generator.html`；L3 深度同看官方 Functional Programming HOWTO（重点 Generators 章节）→ `https://docs.python.org/3/howto/functional.html`；`yield from` 细节查官方 → `https://docs.python.org/3/reference/expressions.html#yield-expressions`

### Day 3 ｜ 8/17 周一 ｜ 上下文管理器 + 类型提示 + dataclass（L2→L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① `with` 原理、`__enter__`/`__exit__`、`contextlib.contextmanager` ② typing：`Optional`/`Union`/`List`/`Protocol` ③ dataclass：字段/`default_factory`/`frozen` | 3h |
| 练习 | 写一个上下文管理器（数据库连接自动关闭）；把 1 个业务 dict 重构为 dataclass 并加类型提示 | 2.5h |
| 自测 | **周中综合小测验**（闭包+装饰器+生成器 10 题，含手写代码题） | 1.5h |
| 产出 | `notes/day03-上下文类型dataclass.md` + 测验成绩 | — |

> **资源**：上下文管理器官方文档 `contextlib` → `https://docs.python.org/3/library/contextlib.html`；dataclass 官方文档 → `https://docs.python.org/3/library/dataclasses.html`；类型提示官方指南 → `https://docs.python.org/3/library/typing.html`（只读 Protocol/Union/Optional 小节）

### Day 4 ｜ 8/18 周二 ｜ Python 异步 + pytest（L2）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① `async`/`await`、事件循环概念、`asyncio.gather`/`create_task` ② `aiohttp` 并发 vs `requests` ③ pytest：`fixture`/`mock`/参数化/覆盖率 | 3h |
| 练习 | 用 `aiohttp` 并发请求 10 个 URL（对比串行耗时）；给 Day 3 的 dataclass 工具函数写 3 个 pytest 用例并跑绿 | 3h |
| 自测 | 解释：协程 vs 线程的区别；事件循环怎么调度；`gather` vs `create_task` | 2h |
| 产出 | `notes/day04-异步测试.md` + 测试用例 | — |

> **资源**：入门读廖雪峰《异步 IO》→ `https://liaoxuefeng.com/books/python/async/index.html`；同步看官方 asyncio 文档（重点 `gather`/`create_task`/`Semaphore` 三个 API）→ `https://docs.python.org/3/library/asyncio.html`；pytest 官方 Getting Started + Fixtures + Parametrize 三节 → `https://docs.pytest.org/en/stable/`

### Day 5 ｜ 8/19 周三 ｜ FastAPI 初体验 + 工程工具链 + Git 规范（L2）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① FastAPI：路径/查询参数、Pydantic 模型、响应模型、自动 Swagger ② venv/conda + pip/poetry + logging ③ Git：分支策略/commit 规范/`.gitignore`/PR 流程 | 3h |
| 练习 | 写一个 todo CRUD 接口（先内存版）：GET/POST/PUT/DELETE + Pydantic 校验，Swagger 页面演示 | 3h |
| 自测 | 按 commit 规范提交今日代码（type(scope): 描述）；`git log` 自查 | 2h |
| 产出 | 第一个 FastAPI 项目（跑通可演示）+ `notes/day05-FastAPI.md` | — |

> **资源**：FastAPI 官方教程（中文，边读边敲）→ `https://fastapi.tiangolo.com/zh/tutorial/`（必读：路径参数、查询参数、请求体、响应模型四节）；Git 原理看《Pro Git》官方中文书第 1-2 章 → `https://git-scm.com/book/zh/v2`；commit 规范原文 → `https://www.conventionalcommits.org/zh-hans/`

### Day 6 ｜ 8/20 周四 ｜ MySQL 深化·上：索引（L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① B+ 树结构原理、聚簇 vs 二级索引、回表 ② 最左前缀、索引失效场景、覆盖索引 | 3h |
| 练习 | 建表（含 10w 条测试数据）→ 建不同类型索引 → `EXPLAIN` 对比走/不走索引 | 3h |
| 自测 | 画出 B+ 树结构图；回答：为什么用 B+ 树不用哈希/红黑树；回表发生在哪 | 2h |
| 产出 | `notes/day06-MySQL索引.md` + explain 分析截图 | — |

> **资源**：小林 coding《图解 MySQL》索引章节（B+树为什么快/聚簇二级索引/最左前缀/覆盖索引）→ `https://xiaolincoding.com/mysql/`；备选视频：B站"尚硅谷 MySQL 高级"（索引部分，只看概念，动手靠自己）

### Day 7 ｜ 8/21 周五 ｜ MySQL 深化·下：SQL 优化 + 事务（L3）+ 周复盘

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① `EXPLAIN` 字段解读（type/key/rows/Extra）、慢查询定位 ② ACID、4 种隔离级别、脏读/不可重复读/幻读、MVCC 原理 | 3h |
| 练习 | 构造 1 个慢查询 → 优化 → `EXPLAIN` 前后对比；验证 2 种隔离级别下的异常现象 | 2h |
| 自测 | 画事务隔离级别与异常对照表；解释 MVCC 怎么实现可重复读 | 1.5h |
| **周复盘** | 填写第七节复盘表（**本周最重要环节，必须完成**） | 1.5h |

> **资源**：小林 coding《图解 MySQL》事务隔离 + MVCC + 锁章节（Next-Key Lock 必看）→ `https://xiaolincoding.com/mysql/`；`EXPLAIN` 官方参考手册 → `https://dev.mysql.com/doc/refman/8.0/en/explain-output.html`

---

## 三、背景线（每天固定）

| 任务 | 量 | 建议时段 |
|------|:---:|------|
| LeetCode 算法 | 1 题/天（先做数组/链表/哈希高频题） | 早起 30-40min |
| 考研英语单词 | 30min/天 | 午/晚碎片 |

---

## 四、本周验收标准（全部满足才算通过）

- [ ] Day 1-3 综合测验 ≥ 8/10（含手写装饰器+生成器代码题）
- [ ] 4 个装饰器 + 生成器练习 + 上下文管理器全部独立写出并 git 提交
- [ ] pytest 3 个用例跑绿，覆盖率 ≥ 80%
- [ ] FastAPI todo 接口 Swagger 可演示
- [ ] `EXPLAIN` 能解读全字段，B+ 树/隔离级别/MVCC 能白板讲清
- [ ] 每日笔记齐 7 篇，周复盘表填写完整
- [ ] 背景线全勤（算法 7 题 + 单词 7 天）

> ⚠️ 任一验收项未过 → 标记到复盘表的"欠账"，顺延到 8/30 机动日补，不拖进 8/22 的 MySQL 收尾。

---

## 五、资源建议（汇总速查）

| 主题 | 资源 |
|------|------|
| Python 进阶 | 廖雪峰教程（装饰器/生成器/异步章节）+ 官方 Functional Programming HOWTO |
| 异步 | 官方 asyncio 文档（gather/create_task/Semaphore） |
| pytest | 官方文档 Getting Started + Fixtures + Parametrize |
| FastAPI | 官方中文教程（路径参数/查询参数/请求体/响应模型） |
| MySQL | 小林 coding《图解 MySQL》（索引/事务隔离/MVCC/锁） |
| Git | Pro Git 中文书 1-2 章 + Conventional Commits 规范 |
| 背景线 | LeetCode 每日一题 + 单词 APP |

> 资源不重要，**输出**才重要：笔记 + 代码 + 测验成绩是本周唯一的"效果凭证"。

---

## 六、Git 提交规范（本周起养成，全程沿用）

```
# 格式
type(scope): 简要描述

# type 示例
feat(api): 新增 todo 查询接口
test(pytest): 补充 dataclass 工具函数用例
docs(notes): day01 装饰器笔记
fix(loop): 修复生成器无限序列边界

# 频率
每天至少 1 次有意义的提交，禁止"一天一坨"大提交
```

---

## 七、周复盘表（8/21 晚上填写）

| 项目 | 记录 |
|------|------|
| 主线完成率（7 天 × 8.5h） | ___% |
| 验收项通过数 | ___ / 7 |
| 欠账清单（没完成/没学会的） | |
| 欠账处理（→ 8/30 机动日补 / 降级 / 放弃） | |
| 每天 8.5h 实际能达到吗？哪个环节超时/浪费 | |
| 本表格式问题反馈（影响继续生成后面 17 周的质量） | |
| 下周继续吗？（Y / 调整后继续 / N） | |
| 本周 3 个最深印象的收获 | 1. 2. 3. |

---

> 📌 试点结论：8/22 晚前把复盘表发给我，或直接告诉我"继续 / 调整 / 停止"。
> 若继续，我将按同样格式生成 Week 2（8/22–8/28：MySQL 收尾 + Redis 深入）及后续各周。
