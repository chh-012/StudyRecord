# 📋 第 2 周执行表（2026/08/22 周六 → 08/28 周五）

> 所属阶段：阶段一·工程底座（收尾：8/22–8/28）
> 本周主题：**MySQL 收尾 + Redis 深入 + Linux 实操 + LLM 首次接触 + 项目 1**
> 依据 Week1 复盘调整：**① 先知识地图再学 ② 项目做综合串联 ③ 回忆式练习（先写后对）④ 时间按 5.5h/天 认账**
> 背景线：算法 1 题 + 单词 30min（每天固定，另算）

---

## 一、Week1 复盘结论 → Week2 怎么改

| Week1 的问题 | Week2 的对应改动 |
|------|------|
| 不知道学的东西拼起来是什么 | **每学一块先看知识地图**，知道它装在系统哪个位置（见第二节） |
| 写之前总瞟一眼正确的才有底气 | **回忆式练习**：合上资料先写 → 对照 → 记差异 → 再写（见第三节） |
| 8.5h/天 撑不住（实际 4-6h） | 改为 **5.5h/天**，省下的时间全投到"闭卷默写"和项目上 |
| 内容东一块西一块 | **项目 1 综合串联**：8/27-28 把本周+上周所有知识拼成一个系统 |

---

## 二、知识地图（本周先看这张图，每天学习前对照）

```
Week2 拼装图：你学的是一个 Web 后端系统的完整骨架
┌─────────────────────────────────────────────────────┐
│  ① 存储层    MySQL 收尾（8/22）                       │
│              └ 索引/事务已会 → 本周：实测优化、隔离级别  │
│  ② 缓存层    Redis（8/23-24）                         │
│              └ 放"热点数据"：查询快 10 倍的那一层       │
│  ③ 部署层    Linux + Shell（8/25-26）                 │
│              └ 项目最终要跑在 Linux 服务器上           │
│  ④ 接入层    LLM API 首接触（8/26）                   │
│              └ Agent 线的引子：9 月全靠它              │
│  ⑤ 拼装      ★ 项目1：知识管理系统（8/27-28）          │
│              FastAPI + MySQL + Redis + pytest + Git   │
│              = 把上面 ①②③ 和 Week1 全部串成能跑的系统 │
└─────────────────────────────────────────────────────┘
```

**为什么按这个顺序**：先补存储层（MySQL 还差最后一块）→ 加缓存层（Redis，8/23-24）→ 部署层（Linux，项目要部署）→ 接 LLM（为 9 月 Agent 预热）→ 最后 2 天把所有零件**拼装**成项目 1。

每天开始学习前，先问自己：**"今天这块装在系统的哪个位置？少了它会怎样？"** 答不上来就看上面的图。

---

## 三、回忆式练习法（本周核心方法，替代"边看边写"）

> 目标：把"瞟一眼才能写"变成"合上也能写"。判断标准不是"能看懂"，而是**"能独立写出来"**。

```
三明治写法（每次练习都按这个流程）：
① 合上所有资料
② 凭记忆写当天练习代码（卡住/写错 → 记进"卡点清单"，不翻资料）
③ 打开资料对照，标出差异（哪里记错了、漏了什么）
④ 合上资料，把差异部分【再写一遍】
⑤ 通过标准：不看资料独立写出当天核心代码

每天结束前 20 分钟「闭卷默写」：
  合上所有资料，默写当天 1-2 个核心代码骨架
  （如 Redis 缓存读写 / Linux 部署命令 / LLM API 调用）
  写不出来 = 明天早上第一件事是复习+重默，而不是学新课
```

**卡点清单**（放笔记顶部，每天记录）：
| 日期 | 卡在哪 | 原因 | 解决后能独立写了吗 |
|------|------|------|------|

---

## 四、每日时间模板（5.5h/天，比 Week1 更聚焦）

| 时段 | 时长 | 内容 |
|------|:---:|------|
| 上午 | 1.5h | **学概念**：看知识地图定位 → 读资料（重点：为什么需要它） |
| 下午 | 2h | **回忆式练习**：三明治写法完成当天练习 |
| 晚上 | 2h | **闭卷默写 20min** + 差异整理 + 卡点清单 + 项目推进 |

> 质量 > 时长：宁可 5.5h 全部"写出来"，不要 8.5h 大部分"看懂了"。

---

## 五、本周每日任务

### Day 1 ｜ 8/22 周六 ｜ MySQL 收尾（L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① `EXPLAIN ANALYZE`（MySQL 8 实测耗时）② 深分页优化（游标 vs 大 OFFSET）③ 慢查询综合排查流程 | 1.5h |
| 练习 | 对照 Week1 的 day06/07 总结：实测加索引前后 EXPLAIN ANALYZE 耗时；开两个会话复现脏读/不可重复读 | 2h |
| 默写 | 闭卷写：`EXPLAIN` 重点字段（type/key/rows/Extra）+ 隔离级别与异常对照表 | 2h |
| 产出 | `_reference/week02` 的 MySQL 收尾总结 + 卡点清单 | — |

> 资源：小林 coding《图解 MySQL》事务/锁章节复习 + MySQL 官方 EXPLAIN 手册

### Day 2 ｜ 8/23 周日 ｜ Redis（上）：5 种数据结构（L2→L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① Redis 是什么、为什么快（内存+单线程）② String/List/Hash/Set/ZSet 及**适用场景** ③ 基本命令 | 1.5h |
| 练习 | 启动 redis-server，用 `redis-cli` 实操 5 种结构各 5 个命令；给每种结构配一个真实场景（如 List=消息队列、ZSet=排行榜） | 2h |
| 默写 | 闭卷写：5 种数据结构 + 各自场景一句话 | 2h |
| 产出 | 卡点清单 | — |

> 资源：Redis 官方命令文档 `https://redis.io/docs/latest/commands/`（中文看菜鸟教程入门）

### Day 3 ｜ 8/24 周一 ｜ Redis（下）：缓存策略 + 持久化（L2→L3）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① 缓存穿透/击穿/雪崩（概念+解决方案）② 过期淘汰策略（LRU/LFU）③ 持久化 RDB vs AOF ④ redis-py 在 Python 中使用 | 1.5h |
| 练习 | 用 `redis-py` 写"查库→写缓存→命中返回"的完整缓存读写；模拟缓存穿透（查不存在 key） | 2h |
| 默写 | 闭卷写：三大缓存问题 + 解决一句话；redis-py 缓存读写代码骨架 | 2h |
| 产出 | 卡点清单（缓存这天的内容 Week2 项目要用） | — |

> 资源：小林 coding《图解 Redis》（缓存三大问题讲得最清楚）

### Day 4 ｜ 8/25 周二 ｜ Linux 实操 + Shell（L2）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① 常用命令（文件/权限/进程/端口）② systemctl 管理服务 ③ 日志查看（journalctl/tail）④ Shell：变量/循环/条件 | 1.5h |
| 练习 | 在 Linux（虚拟机/WSL/云服务器均可）上：创建用户+权限、后台运行一个服务、写一个 Shell 脚本定时清理日志 | 2h |
| 默写 | 闭卷写：部署一个 FastAPI 的完整命令序列（从上传到启动） | 2h |
| 产出 | 卡点清单 | — |

> 资源：《鸟哥的 Linux 私房菜》基础篇（挑章节看）+ 菜鸟教程 Shell

### Day 5 ｜ 8/26 周三 ｜ Linux 收尾 + LLM 首次接触（L1→L2）

| 环节 | 内容 | 预计 |
|------|------|:---:|
| 学习 | ① 在 Linux 上部署 Week1 的 FastAPI todo（实战）② 注册一个 LLM API（DeepSeek/通义/OpenAI 任一）③ 跑通第一个对话请求 | 1.5h |
| 练习 | 完成部署 + 用 Python 发第一次 LLM 请求（设置 API Key → 调用 → 打印回复） | 2h |
| 默写 | 闭卷写：LLM API 调用代码骨架（含 API Key 放 .env 不硬编码） | 2h |
| 产出 | **第一个 .env**（放 API Key，确认 .gitignore 已忽略）+ 卡点清单 | — |

> ⚠️ 红线：API Key 只放 `.env`，`.gitignore` 已配好，push 前自查 `git status` 无 `.env`

### Day 6-7 ｜ 8/27-28 ｜ ★ 项目 1：知识管理系统（综合串联，L2）

> 这是本周（和上周）所有知识的**拼装验证**——做的时候对照知识地图：每个零件来自哪一天？

| 环节 | 8/27（Day 6） | 8/28（Day 7） |
|------|------|------|
| 上午 1.5h | 表设计（笔记表+标签表）+ FastAPI 骨架 + 建库 | 搜索接口（走 Redis 缓存）+ pytest 3 个用例 |
| 下午 2h | 笔记 CRUD（增删改查 + Pydantic） | 缓存读写完善 + 错误处理 + README |
| 晚上 2h | 联调 Swagger + 提交 git | 部署到 Linux（Day4-5 学的）+ **周复盘** |

**项目 1 完成定义**：CRUD 全通 + 搜索走缓存 + pytest 跑绿 + Swagger 可演示 + git 已提交 + 能对着知识地图讲"每层用了哪天的知识"。

---

## 六、本周验收标准（全勾 = 通过）

- [ ] 闭卷默写 7 天全过关（能独立写出当天核心代码骨架）
- [ ] Redis：能独立写缓存读写代码，能讲清穿透/击穿/雪崩区别
- [ ] Linux：能独立完成 FastAPI 部署命令序列
- [ ] LLM：第一个 API 调用跑通，Key 在 `.env` 未被提交
- [ ] **项目 1 完成定义全满足**（见上）
- [ ] 卡点清单 7 天有记录，卡点都解决了
- [ ] 背景线全勤（算法 7 题 + 单词 7 天）

---

## 七、周复盘表（8/28 晚上填写）

| 项目 | 记录 |
|------|------|
| 主线完成率（7 天 × 5.5h） | ___% |
| 闭卷默写通过率 | ___ / 7 天 |
| 卡点清单解决了几个 / 遗留几个 | ___ / ___ |
| **系统感自评**：能说清每个知识点装在系统哪个位置吗？（比 Week1 有改善？） | |
| 回忆式练习法有效吗？还瞟答案吗？ | |
| 项目 1 拼装顺利吗？哪块最吃力（= 哪块没学扎实）？ | |
| 本表格式反馈 | |
| 下周继续吗？（Y / 调整 / N） | |
| 本周 3 个收获 | 1. 2. 3. |

---

> 📌 8/29 晚前把复盘表发我，或直接说"继续"。Week3 将按同样结构（知识地图 + 回忆式 + 项目串联）生成，并开始阶段二（Agent 线 + Java 线双线并行）。
