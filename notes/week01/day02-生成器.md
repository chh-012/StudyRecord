# Day02 · 生成器与迭代器

> 日期：2026-08-16 ｜ 深度目标：L3 ｜ 状态：□ 未完成 □ 已完成
> ⚠️ 先自己写，写完全部后再对照 `_reference/week01/day02-生成器.md`

## 1. 核心概念

用自己的话写 2-3 句（写不出来 = 没学会）：

- 迭代器协议是什么？可迭代对象和迭代器的区别？
- 含 `yield` 的函数调用后返回什么？为什么说它是"惰性"的？
- `for` 循环内部是怎么工作的？

## 2. 关键代码

自己敲，跑通才算会（提交 git）：

- [ ] 斐波那契生成器（`next()` 取前 8 个）
- [ ] 大文件流式逐行读取（对比 `read()` 一次性载入）
- [ ] `send()` 协程雏形：外部向生成器传值
- [ ] `yield from` 委托子生成器
- [ ] 实测：生成器 vs list 的内存对比（用 `sys.getsizeof` 或实际大循环）

## 3. 原理图

画两张图：

- 生成器生命周期：创建 → next → 暂停 → next → StopIteration
- 内存对比：list 一次装 vs 生成器逐个生产

## 4. 面试问答

自己先答，答不出再翻 _reference：

- Q1：`range(10)` 和 `list(range(10))` 的区别？
- Q2：生成器为什么只能迭代一次？
- Q3：`send()` 有什么用？
- Q4：`yield` 和 `return` 能同时用吗？
- Q5：迭代器为什么需要 `__iter__` 返回自身？

## 5. 踩坑记录

| 坑 | 现象 | 解决 |
|------|------|------|
| （今天真实报错，逐条记） | | |

## 6. 待补 / 明日预告

- [ ] （学完补自己的待补项）
- 明日：上下文管理器 + 类型提示 + dataclass
