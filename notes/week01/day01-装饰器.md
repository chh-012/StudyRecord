# Day01 · Python 装饰器与函数进阶

> 日期：2026-08-15 ｜ 深度目标：L3 ｜ 状态：□ 未完成 □ 已完成
> ⚠️ 先自己写，写完全部后再对照 `_reference/week01/day01-装饰器.md`

## 1. 核心概念

用自己的话写 2-3 句（写不出来 = 没学会）：

- 函数一等公民是什么意思？
- 什么是闭包？外层函数返回后，被引用的变量为什么还在？
- 装饰器的本质是什么？`@deco` 等价于哪行代码？
- `functools.wraps` 解决什么问题？

## 2. 关键代码

自己敲，跑通才算会（全部提交 git，commit 规范见 day05）：

- [ ] 日志装饰器：打印函数名和参数
- [ ] 执行计时装饰器
- [ ] 带参重试装饰器（最多重试 N 次，含参数）
- [ ] 缓存装饰器（用 `functools.lru_cache` 或手写）
- [ ] 验证：`hello.__name__` 是否仍是 `hello`（wraps 的效果）

## 3. 原理图

画两张图：

- `@deco` 等价于 `f = deco(f)` 的调用链（调用 f(x) 时经过哪些层）
- 带参装饰器 `@retry(3)` 的三层结构

## 4. 面试问答

自己先答，答不出再翻 _reference：

- Q1：多个装饰器 `@a @b` 的执行顺序？
- Q2：`@wraps` 为什么必要？
- Q3：装饰器和装饰器工厂的区别？
- Q4：闭包为什么能记住外层变量？

## 5. 踩坑记录

| 坑 | 现象 | 解决 |
|------|------|------|
| （今天真实报错，逐条记） | | |

## 6. 待补 / 明日预告

- [ ] （学完补自己的待补项）
- 明日：生成器与迭代器
