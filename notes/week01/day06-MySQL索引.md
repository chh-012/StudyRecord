# Day06 · MySQL 深化（上）：B+ 树索引

> 日期：2026-08-20 ｜ 深度目标：L3 ｜ 状态：□ 未完成 □ 已完成
> ⚠️ 先自己写，写完全部后再对照 `_reference/week01/day06-MySQL索引.md`

## 1. 核心概念

用自己的话写 2-3 句（写不出来 = 没学会）：

- B+ 树索引为什么适合磁盘？叶子节点链表有什么好处？
- 聚簇索引和二级索引的区别？回表是什么？
- 最左前缀原则具体指什么？
- 覆盖索引为什么快？

## 2. 关键代码

自己敲，跑通才算会（提交 git——SQL 脚本存 `.sql` 文件）：

- [ ] 建 `users` 表（utf8mb4）+ 造 10w 条测试数据
- [ ] 建单列索引 `idx_age`、复合索引 `idx_name_age`
- [ ] 用 `EXPLAIN` 验证：等值查询走 ref、范围查询走 range
- [ ] 复现最左前缀：只用 age 查复合索引时是否走索引
- [ ] 复现覆盖索引：只查索引列出现 `Using index`
- [ ] 复现索引失效：`LEFT(name,5)` 函数包裹索引列

## 3. 原理图

画 3 层 B+ 树示意（非叶子只存键、叶子存数据/主键、叶子间双向链表），标注"查询 3 次磁盘 IO"

## 4. 面试问答

自己先答，答不出再翻 _reference：

- Q1：为什么 MySQL 用 B+ 树不用哈希/红黑树/B 树？
- Q2：什么是回表？怎么避免？
- Q3：最左前缀原则？
- Q4：哪些操作会让索引失效？
- Q5：`EXPLAIN` 重点看哪些字段？`Using filesort` 意味着什么？

## 5. 踩坑记录

| 坑 | 现象 | 解决 |
|------|------|------|
| （今天真实报错，逐条记） | | |

## 6. 待补 / 明日预告

- [ ] （学完补自己的待补项）
- 明日：MySQL 深化（下）—— SQL 优化 + 事务隔离 + MVCC
